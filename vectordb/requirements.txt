# VectorDB dependencies
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
faiss-cpu>=1.8.0
numpy>=1.26.0
httpx>=0.27.0        # client library + async support

# Optional — required only for /search/by-text
sentence-transformers>=3.0.0

# Dev / test
pytest>=8.2.0
pytest-asyncio>=0.23.0
